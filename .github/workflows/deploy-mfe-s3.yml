name: MFE S3 Bucket Deployment 🚀

on:
  push:
    branches:
      - open-release/redwood.nelp
      - open-rc/redwood.nelp

  pull_request:
    branches:
       - "**open-rc**"

jobs:
  deploy:
    uses: nelc/actions-hub/.github/workflows/mfe-s3-bucket-deployment.yml@afg/fix-reusable-actions
    with:
     PROD_BRANCH: 'open-release/redwood.nelp'
    secrets: inherit
